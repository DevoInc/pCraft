ami_version 1
+++++++
* Global Variables:
    gv:[0] $simple = simple
    gv:[1] $verbatimsub = This is not a simple
verbatim string here.
We can use variable substitution,
such as escape:escaped "str" or simple:simple
    gv:[2] $verbatim2 = verbatim "other" string
    gv:[3] $verbatim = verbatim 
string
$simple
on multiple lines
    gv:[4] $harder = harder than simple
    gv:[5] $escape3 = This is an "escaped" string
    gv:[6] $escaped = escaped "str"

